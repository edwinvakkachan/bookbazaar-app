<!-- File: views/admin/partials/footer.ejs -->
      </main>
    </div>
  </div>

  <!-- alert box -->
<div class="modal fade" id="popupAlert" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header bg-dark text-white">
        <h5 class="modal-title" id="popupAlertTitle">Notification</h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body" id="popupAlertMessage"></div>
      <div class="modal-footer">
        <button type="button" class="btn btn-dark" data-bs-dismiss="modal">OK</button>
      </div>
    </div>
  </div>
</div>


<script src="/js/alert.js"></script>
<script src="/js/confirmModal.js"></script>



  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <%- include('../partials/admin/confirmModal') %>

  <script>
    // Auto-highlight active link if not passed via activePage
    (function() {
      try {
        var path = location.pathname;
        if (!document.querySelector('.nav-link.active, .list-group-item.active')) {
          document.querySelectorAll('.admin-sidebar .nav-link, #mobileSidebar .list-group-item').forEach(function(a){
            var href = a.getAttribute('href');
            if (href && (path === href || path.startsWith(href + '/'))) {
              a.classList.add('active');
            }
          });
        }
      } catch(e) { /* no-op */ }
    })();
  </script>

  <!-- Optional per-page scripts -->
  <% if (typeof scripts !== 'undefined') { %>
    <%- scripts %>
  <% } %>
</body>
</html>
