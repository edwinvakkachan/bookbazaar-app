<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Log in to Exclusive - BookBazaar</title>
  
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>

    .google-icon {
      width: 20px;
      height: 20px;
      background: conic-gradient(from -45deg, #ea4335, #4285f4, #34a853, #fbbc05, #ea4335);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .google-icon::after {
      content: 'G';
      color: white;
      font-weight: bold;
      font-size: 12px;
    }
  </style>
</head>
<body class="bg-light d-flex align-items-center justify-content-center vh-100">

  <div class="container">
    <div class="row shadow bg-white overflow-hidden" style="max-width:900px; margin:auto; min-height:600px;">
      
      <!-- Left side - Image section -->
      <div class="col-md-6 bg-dark text-white position-relative d-flex align-items-center justify-content-center p-0">
        <!-- Placeholder for image -->
        <div class="d-flex align-items-center justify-content-center w-100 h-100 bg-secondary bg-gradient text-muted">
          <!-- <img src="/images/signup.png" alt="" class="img-fluid w-100 h-100"> -->
          [Your image will go here]
        </div>
        <!-- Logo overlay -->
        <div class="position-absolute bottom-0 mb-4 text-center w-100">
          <h2 class="fw-bold display-6 mb-1">BB</h2>
          <p class="fw-light fs-5">BOOKBAZAAR</p>
        </div>
      </div>

      <!-- Right side - Form section -->
      <div class="col-md-6 d-flex flex-column justify-content-center p-5">
        <h1 class="h3 mb-2">Log in to Exclusive</h1>
        <p class="text-muted mb-4">Enter your details below</p>

        <form id="loginForm" action="/login" method="POST">
          <!-- Email -->
          <div class="mb-3">
            <input type="email" class="form-control border-0 border-bottom" placeholder="Email" name="email" id="email" >
          </div>

          <!-- Password -->
          <div class="mb-3">
            <input type="password" class="form-control border-0 border-bottom" placeholder="Password" name="password" id="password" >
          </div>

          <% if (locals.message && message.length > 0) { %>
            <div class="alert alert-danger text-center"><%= message %></div>
          <% } %>

          <!-- Forgot password -->
          <div class="text-end mb-3">
            <a href="/forgotPassword" class="text-danger text-decoration-none" onclick="forgotPassword()">Forget Password?</a>
          </div>

          <!-- Login button -->
          <button type="submit" class="btn btn-dark w-100 mb-3">Login</button>

        
          <button type="button" class="btn btn-outline-secondary w-100 mb-4 d-flex align-items-center justify-content-center gap-2" onclick="signInWithGoogle()">
            <div class="google-icon"></div>
            Sign in with Google
          </button>

          <!-- Signup link -->
          <div class="text-center">
            <small class="text-muted">Don't have an account? <a href="/signup" class="fw-semibold text-decoration-none">Sign Up</a></small>
          </div>
        </form>
      </div>

    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
